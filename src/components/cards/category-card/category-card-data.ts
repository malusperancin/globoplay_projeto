type CategoryCardData = {
  UI_number: number;
  backgroundUrl: string;
  posterUrl: string;
  headline: string;
  displayName?: string;
  id: string;
  cover: {
    landscape: string;
  };
  UI_customHeight: string;
};

export default CategoryCardData;
